[core]
    pager = less -F
    quotePath = false

[alias]
    ad = add
    br = branch
    bra = br -a
    ci = commit
    cloneb = clone --filter blob:none
    di = diff
    dis = di --staged
    dist = dit --staged
    dit = difftool
    ff = merge --ff-only
    gr = log --graph --pretty=\"%>|(31)%ai %C(blue bold)%<(10)%an%C(auto) %h %s%d\"
    gra = gr --branches --tags --remotes
    me = merge
    rs = restore --staged
    ss = stash
    st = status
    sw = switch

[color]
    ui = auto

[commit]
    gpgsign = true

[credential]
    helper = libsecret

[diff]
    algorithm = histogram
    tool = nvimdiff

[difftool]
    prompt = false

[difftool.nvimdiff]
    cmd = nvim -dR "${LOCAL}" "${REMOTE}"

[fetch]
    prune = true
    pruneTags = true

[init]
    defaultBranch = master

[merge]
    conflictStyle = diff3
    ff = false

[pull]
    ff = only

[push]
    followTags = true

[user]
    name = syuzuki
    email = 77492100+syuzuki@users.noreply.github.com
    signingkey = 551741641F3BF84C

[include]
    path = config.local
